# SCIRS — Bug Log

Maintained per `testing-standards.md`. The rubric rewards bugs being
identified and resolved — this is the evidence.

| ID | Description | Module | Severity | How Found | Root Cause | Fix | Commit |
|----|-------------|--------|----------|-----------|------------|-----|--------|
| BUG-01 | `AuthController.login` and `.me` returned 500 instead of 200 | Auth | High | `AuthControllerIntegrationTest` failing with `LazyInitializationException` | `User.role` is `FetchType.LAZY`; `AuthService.login()`/`.me()` were not `@Transactional`, and `spring.jpa.open-in-view=false` closes the Hibernate session before the mapper reads `user.getRole()` | Annotated both methods `@Transactional(readOnly = true)` | 3955ae6 |
| BUG-02 | `DepartmentRepository`/`CategoryRepository` failed application startup with `PropertyReferenceException: No property 'isActive' found for type 'Category'` | Department/Category | High | `DepartmentControllerIntegrationTest` failing to load the Spring context | Both entities store the flag as a field named `active` (JavaBean property `active`, getter `isActive()`); the derived query method was named `findByIsActiveTrue()`, which Spring Data parses as property `isActive` — a property that does not exist | Renamed both repository methods to `findByActiveTrue()`; corrected `database-schema.md` to match | (pending commit) |
| BUG-03 | `GET /api/categories` returned 500 instead of 200 | Category | High | `CategoryControllerIntegrationTest` failing with `LazyInitializationException` | `Category.department` is `FetchType.LAZY`; `CategoryService.getAll()`/`.getById()` were not `@Transactional`, and `spring.jpa.open-in-view=false` closes the Hibernate session before `CategoryMapper` reads `category.getDepartment().getName()` | Annotated both methods `@Transactional(readOnly = true)` | (pending commit) |
| BUG-04 | `POST /api/reports` with an image over `spring.servlet.multipart.max-file-size` (5 MB) returned 500 instead of the 413 documented in `api-standards.md` | Report | Medium | Written while implementing Phase 4 image upload; Spring's multipart resolver rejects oversized files before the request reaches the controller, throwing `MaxUploadSizeExceededException`, which `GlobalExceptionHandler` had no handler for (fell through to the generic `Exception` → 500 handler) | `GlobalExceptionHandler` had no `@ExceptionHandler(MaxUploadSizeExceededException.class)` case | Added a handler mapping `MaxUploadSizeExceededException` to `413 Payload Too Large` | (pending commit) |
| BUG-05 | `mvn spring-boot:run` failed application startup with `io.jsonwebtoken.security.WeakKeyException: ... 0 bits ...` | Auth/Config | High | Reported by user as a build failure | `jwt.secret=${JWT_SECRET:}` in `application.properties` intentionally defaults to empty (secret must come from a git-ignored local source), but no `backend/.env` or `application-local.properties` had been created yet on this machine, so `JwtUtil`'s constructor received an empty string and `Keys.hmacShaKeyFor` rejected the resulting 0-bit key | Created git-ignored `backend/.env` with a generated 256-bit `JWT_SECRET`, matching the mechanism `application.properties` already documents (`spring.config.import=optional:file:.env[.properties]`) | (not committed — `.env` is git-ignored) |
| BUG-06 | `DataSeeder` failed at startup with `org.postgresql.util.PSQLException: ERROR: prepared statement "S_1" already exists` | Config/Data | High | Surfaced immediately after fixing BUG-05, once the app could reach the DB | `SPRING_DATASOURCE_URL` points at Supabase's connection pooler on port 6543 (PgBouncer in transaction-pooling mode), which does not support server-side prepared statements; pgjdbc's statement cache tried to reuse a server-side prepared statement name (`S_1`) across pooled connections, which PgBouncer rejects | Appended `&prepareThreshold=0` to `SPRING_DATASOURCE_URL` to force pgjdbc to always use client-side (unnamed) statements | (not committed — `.env` is git-ignored) |
| BUG-07 | New `MockDataSeeder.run()` failed with `org.hibernate.LazyInitializationException: could not initialize proxy [Department#1] - no Session` on its first report | Config/Data | Medium | Live run against the Supabase dev DB (`SEED_MOCK_DATA=true`), first attempt | `CommandLineRunner.run()` is not `@Transactional` by default; `category.getDepartment()` (`Category.department` is `FetchType.LAZY`) was called after the `categoryRepository.findAll()` session had already closed | Annotated `MockDataSeeder.run()` `@Transactional` so the whole seeding pass shares one Hibernate session | (pending commit) |
| BUG-08 | `mvn test` failed every service-layer test with `MockitoException: Could not modify all classes` under a JDK newer than 21 (this machine only has JDK 25/26 installed, no JDK 21) | Build/Test | High | Running the full suite for Phase 9 test verification | Mockito's inline mock maker (ByteBuddy) does not recognise class file versions emitted by JDK 22+ unless told to accept them; the fix noted in the 2026-08-16 `progress-tracker.md` decision (switch to a JDK 21 `JAVA_HOME`) turned out to be unnecessary — a JVM system property is sufficient and works on any JDK | Ran tests with `-DargLine="-Dnet.bytebuddy.experimental=true"` (`mvn test -DargLine=...`, or add `<argLine>-Dnet.bytebuddy.experimental=true</argLine>` to the surefire plugin config in `pom.xml` to make it permanent) — no JDK downgrade needed | (pending commit) |
| BUG-09 | `ReportAssignmentServiceTest.assign_withInactiveDepartment_throwsBusinessRuleException` failed: threw `ResourceNotFoundException("User not found with id: 99")` instead of the expected `BusinessRuleException` | Report | Medium | Full `mvn test` run for Phase 9 (surfaced only once BUG-08 was worked around and the suite could actually execute this test) | `ReportAssignmentService.assign()` resolved the acting admin user (`findUser(admin.getId())`, only needed to attribute the final audit-log row) *before* validating the target department/staff, so any validation failure on a request whose JWT `admin.getId()` wasn't stubbed in the test surfaced the wrong exception — and in production, a request against a real admin id would never trigger it, masking the ordering bug outside of tests | Moved `findUser(admin.getId())` to immediately before `reportRepository.save()`, after every business-rule check; removed the two now-genuinely-unnecessary `userRepository.findById(99L)` stubs this exposed in `assign_withNonStaffUserAsStaffId_throwsBusinessRuleException` and `assign_withStaffFromAnotherDepartment_throwsBusinessRuleException` (Mockito's strict stubbing correctly flagged them once the ordering fix made them unreachable) | (pending commit) |
| BUG-11 | Citizen reports submitted near an existing report never appeared in the admin dashboard "Reports awaiting approval" panel or the Report Approvals tab | Report (frontend) | High | Reported by user ("make sure that when a citizen posts a report it also shows in the admin dashboard to wait for approval") | Not a backend defect: `POST /api/reports` runs a proximity duplicate check and, when nearby same-category reports exist, returns `200 OK` with a `DuplicateCheckResultDTO` and persists nothing (api-standards.md § "Submit a report — duplicate check"). `citizenReportApi.submitReport()` unconditionally treated the response as the created `ReportDTO`, so the form reported success and navigated to `/report/undefined`; the report was never created and so never entered the `PENDING_APPROVAL` queue the dashboard and approvals tab read from | Normalised `submitReport()` to return `{ outcome, report?, possibleDuplicates? }`; `useNewReportForm` now surfaces a `DuplicateCheckDialog` ("Is this the same issue?") letting the citizen confirm an existing report (`confirmDuplicateOfId`) or force a new one (`forceCreate: true`). Added `citizenReportApi.test.js` covering all three outcomes | (pending commit) |
| BUG-10 | Could not log in as `citizen1@example.com` / `staff.roads@scirs.gov` ("Invalid email or password"), and the admin reports list was empty even after a citizen submitted a report | Config/Data | Medium | Reported by user against a fresh local database | Not a code defect: `MockDataSeeder` is gated by `app.mock-data.enabled` (`SEED_MOCK_DATA`), which defaults to `false`, so the demo citizens, staff, and 30 sample reports had never been created on this machine. The only real accounts were the seeded admin plus a few manually-registered test citizens; `citizen*@example.com` and `staff.*@scirs.gov` simply did not exist, and there were zero reports for the admin to see | Set `SEED_MOCK_DATA=true` in the git-ignored `backend/.env` and restarted once; `MockDataSeeder` created the demo users (citizen passwords `Citizen@12345`, staff `Staff@12345`) and sample reports. Verified end-to-end: citizen/staff login, citizen report submission, and admin visibility of all reports via `GET /api/reports` | (not committed — `.env` is git-ignored) |
| BUG-12 | The console Reports list (`/staff/reports`, `/admin/reports`) showed only "An unexpected error occurred"; `GET /api/reports` returned 500 for every request except one that happened to pass `status`, `startDate` and `endDate` together | Report | High | Reported by user (screenshot of the staff Reports page); reproduced against the live PostgreSQL database | The new paged `ReportRepository.searchReports` query used the project's usual `(:param IS NULL OR column = :param)` idiom for the `search`, `from` and `to` filters. That idiom is safe for the enum/`Long` filters, but for a null `String` or `LocalDateTime` bind Hibernate 6.5 emits an *untyped* parameter: PostgreSQL then infers `bytea` and aborts with `function lower(bytea) does not exist` (search) or `cannot cast type bytea to timestamp without time zone` (dates). H2 silently tolerates the untyped binds, so every integration test passed and the fault only appeared against PostgreSQL. Wrapping the binds in `CAST(... AS ...)` just moved the error | Made the query take only non-null `searchPattern`/`from`/`to`: `ReportService.getReports` now substitutes `"%"` (matches every row) and open-ended sentinel dates (`1970-01-01` … `9999-12-31`) when the caller omits a filter, and the JPQL matches with `LOWER(r.reportCode) LIKE :searchPattern OR LOWER(r.title) LIKE :searchPattern` / `r.createdAt >= :from AND r.createdAt < :to` — no `IS NULL` test over a nullable String/date bind. `search` is lowercased and `%`-wrapped service-side. Added two `ReportControllerIntegrationTest` cases (paged envelope + department scoping, case-insensitive search) | (pending commit) |
| BUG-13 | Admin dashboard: the "Total citizens" and "Total reports" stat cards always read 0, and "Recent registrations" listed already-approved citizens with Approve/Deny buttons that could only ever fail | Dashboard | Medium | Reported by user (screenshot from a groupmate) | `GET /api/dashboard/admin` (`AdminDashboardDTO`) never carried `totalCitizenCount`/`totalReportCount` — the frontend `STAT_CARDS` read those keys and fell back to `?? 0` (the frontend author had left a "Revisit once the real DTO ships" note). Separately, `DashboardService.getAdminDashboard()` filled `recentRegistrations` with the 10 most recent `CITIZEN` accounts *regardless of `accountStatus`*, but `RecentRegistrationsPanel` renders inline approve/deny, and `/users/{id}/approve`/`reject` return `400` for any non-`PENDING` target | Backend: added `totalCitizenCount` (`userRepository.countByRoleName(CITIZEN)`) and `totalReportCount` (`reportRepository.count()`) to `AdminDashboardDTO`; scoped `recentRegistrations` to pending citizens via new `findTop10ByRoleNameAndAccountStatusOrderByCreatedAtDesc(CITIZEN, PENDING)`. Frontend: `RecentRegistrationsPanel` now defensively filters to `accountStatus === "PENDING"`. Updated `api-standards.md` example body, `DashboardServiceTest`, `DashboardControllerIntegrationTest` | (pending commit) |
